# Excel 信息快速检索工具 PRD

## 1. 文档信息

- 产品名称：Excel 信息快速检索工具（暂定）
- 文档版本：V0.1
- 使用对象：个人用户
- 运行环境：Windows，本机已安装 Microsoft Excel
- 文档状态：需求初稿

## 2. 产品背景

用户需要从一个或多个 Excel 文件中快速查找指定信息，并查看命中单元格所在的完整行。现有的 Excel 手工查找在关键词较多、工作表较多或需要汇总导出时效率较低。

本产品定位为完全本地运行的极简桌面工具。文件内容不上传到服务器；源文件默认只读；用户可以预览匹配结果，并将结果另存为新的 Excel 文件。

## 3. 产品目标

1. 支持使用一个或多个关键词检索 Excel 中的信息。
2. 支持“同时满足全部关键词”和“满足任意关键词”两种组合规则。
3. 支持精确匹配和包含匹配。
4. 搜索全部工作表、全部有内容的单元格，不依赖固定表头或固定数据区域。
5. 命中后展示命中单元格所在的完整物理行。
6. 支持在应用内预览结果，并导出为新的 Excel 文件。
7. 导出时尽可能完整保留原始 Excel 的内容与格式。
8. 保持界面极简，降低日常使用成本。

## 4. 非目标

V1 暂不包含以下能力：

- 修改源 Excel 内容。
- 云端上传、云端同步或多人协作。
- 用户账号、权限管理和操作审计。
- OCR、图片文字识别或扫描件检索。
- 正则表达式、高级模糊匹配和语义搜索。
- 将表头识别作为搜索成立的前提。

## 5. 用户与使用场景

### 5.1 用户

产品仅供开发者本人使用，输入文件均为用户信任的文件。

### 5.2 典型场景

1. 用户选择一个或多个待检索的 Excel 文件。
2. 应用在本地读取并准备可搜索数据。
3. 用户在极简搜索框中输入一个或多个关键词。
4. 用户选择关键词组合规则及匹配方式。
5. 应用展示匹配行，并标明来源文件、工作表和原始行号。
6. 用户选择导出，指定保存路径，应用生成新的 Excel 文件。

## 6. 核心产品规则

### 6.1 文件准备

- 用户可在开始搜索前选择需要处理的 Excel 文件。
- 多文件选择、文件预处理、索引复用及文件更新检测的具体方案单独建立 issue 细化。
- V1 优先支持 Excel Open XML 格式，包括 `.xlsx` 和 `.xlsm`；其他格式是否纳入首版由技术验证决定。
- 源文件始终以只读方式处理，不原地覆盖。

### 6.2 搜索范围

- 默认搜索所有选中文件。
- 默认搜索文件中的全部工作表。
- 默认搜索工作表内所有有内容的单元格。
- 不要求表头位置固定，也不要求数据区域固定。
- 同一工作表可以包含一个或多个相互独立的表格。
- 命中结果以 Excel 的物理行号为基础，不依赖应用推断表格边界。

### 6.3 关键词输入

- 支持输入一个或多个关键词。
- V1 应提供明确且低成本的多关键词录入方式，例如按 Enter 添加关键词。
- 应支持删除单个关键词及一键清空。
- 空关键词不参与匹配。
- 关键词前后的无意义空白默认忽略。

### 6.4 关键词组合规则

用户可在以下两种规则间切换：

- 全部满足（AND）：同一物理行中必须能找到全部关键词，关键词可以分别命中该行的不同单元格。
- 任意满足（OR）：同一物理行中至少找到一个关键词即可。

只有一个关键词时，两种规则的结果应一致。

### 6.5 匹配方式

用户可在以下两种方式间切换：

- 精确匹配：单元格标准化后的完整显示文本与关键词一致。
- 包含匹配：单元格标准化后的显示文本中包含关键词。

V1 默认不区分英文字母大小写。数值、日期、布尔值和公式结果按 Excel 中可见的显示值参与搜索。该标准化规则需在技术设计阶段形成可测试定义。

### 6.6 结果去重与排序

- 同一物理行命中多个关键词或多个单元格时，在结果列表中只显示一次。
- 每条结果必须记录：来源文件、工作表名称、原始行号、命中的关键词及命中列。
- 默认按文件选择顺序、工作表顺序、原始行号升序排列。

## 7. 结果预览

- 搜索完成后在应用内展示匹配行。
- 预览必须包含来源文件、工作表名称和原始行号，避免多表场景下无法定位。
- 预览展示该物理行在工作表已使用列范围内的全部单元格内容。
- 应对超宽工作表提供横向滚动。
- 应显示结果总数和搜索状态。
- 无结果时显示明确的空状态，不生成空白导出文件。
- 搜索期间界面不得失去响应，并应允许用户取消搜索。

## 8. 导出

### 8.1 导出流程

1. 用户点击“导出”按钮。
2. 系统打开 Windows 保存文件对话框。
3. 用户选择保存目录和文件名。
4. 系统生成新的 Excel 文件，不修改源文件。
5. 完成后显示保存路径及导出结果数量。

### 8.2 导出结构

- 每个来源工作表对应结果文件中的独立工作表；若涉及多个源文件，应通过安全且可辨认的工作表命名规则避免重名。
- 每个结果工作表包含该来源工作表命中的物理行。
- 结果行保留原始顺序，并记录可追溯的源文件、源工作表和原始行号。
- 因为表头和数据区域不固定，V1 不自动推断或额外插入表头；需要保留哪些上下文行应在技术原型验证后确定。

### 8.3 格式保留范围

目标是使用本机 Excel 自动化能力最大程度保留：

- 单元格值、公式及显示格式。
- 字体、字号、颜色、填充、边框、对齐和数字格式。
- 行高、列宽、隐藏状态及冻结窗格。
- 合并单元格。
- 条件格式、数据验证和批注。
- 图片、图表、形状、数据透视表、外部链接和宏。

由于“仅抽取命中行”会改变工作表结构，跨行合并、跨行公式、图表数据源、数据透视表、条件格式区域等对象可能无法在新工作簿中同时保持原有语义。开发前必须通过真实样本完成格式保真原型验证，并确定以下导出策略之一：

1. 抽取模式：仅复制命中行，优先保证结果简洁。
2. 原貌模式：复制完整工作表并隐藏或筛除未命中行，优先保证格式和对象完整。

在策略确定前，“全部保留”作为产品目标，不作为未经样本验证的绝对承诺。

## 9. 界面要求

界面采用单窗口、极简布局，首屏聚焦以下控件：

- 文件选择入口。
- 关键词输入框。
- AND / OR 分段选择控件。
- 精确匹配 / 包含匹配分段选择控件。
- 搜索按钮。
- 结果预览区。
- 导出按钮。

没有可搜索文件或没有有效关键词时，搜索按钮不可用；没有搜索结果时，导出按钮不可用。

## 10. 非功能要求

### 10.1 隐私与安全

- 应用完全在本地运行，不上传文件内容。
- 默认不保存关键词、搜索结果或单元格内容日志。
- 源文件只读，导出文件由用户主动指定保存位置。
- 自动化 Excel 时默认不主动执行宏或更新外部链接。

### 10.2 性能与容量

- 产品不设置人为的文件大小、行数或工作表数量上限。
- 实际容量受 Microsoft Excel 格式上限、本机内存、磁盘空间和处理时间约束。
- 对大型文件应提供可见进度、取消操作和错误提示，不能静默卡死。
- 可通过预处理或本地索引提升重复搜索速度，具体方案在独立 issue 中定义。

### 10.3 兼容性

- 目标系统为 Windows。
- 依赖用户本机安装的桌面版 Microsoft Excel。
- 首版具体支持的 Windows 与 Office 版本需在开发环境确认后写入发布说明。

### 10.4 稳定性

- 单个文件处理失败时，应说明失败文件及原因，不影响其他已选文件的结果。
- 应处理 Excel 弹窗、文件占用、受保护视图和残留 Excel 进程等异常场景。
- 导出失败不得生成看似成功但内容不完整的结果文件。

## 11. 验收标准

1. 用户可以选择至少一个受支持的 Excel 文件并完成本地搜索。
2. 输入多个关键词后，AND 模式只返回同时包含全部关键词的物理行。
3. OR 模式返回包含任意关键词的物理行，且同一行只出现一次。
4. 精确匹配不会返回仅包含关键词但不完全相等的单元格。
5. 包含匹配可以返回文本中包含关键词的单元格。
6. 搜索覆盖所有工作表，不依赖固定表头或固定数据区域。
7. 同一工作表存在多个独立表格时，命中结果仍能按物理行正确预览。
8. 每条预览结果均可追溯到来源文件、工作表和原始行号。
9. 用户可通过系统保存对话框指定导出文件路径。
10. 导出不会修改源文件，且结果行与预览结果一致。
11. 常规单元格格式、行高和列宽在导出结果中与源文件一致。
12. 大型文件搜索期间界面保持响应，用户可以取消操作。

## 12. 技术方向（非最终设计）

- 桌面界面：PySide6。
- 搜索层：通过批量读取或本地索引获取可搜索的显示值，避免逐单元格 COM 调用造成明显性能问题。
- 导出层：使用 Windows Excel COM 自动化，由 Microsoft Excel 执行工作表或行的复制与保存，提高格式保真度。
- 采用搜索与导出分层的混合实现，具体组件和数据结构由技术设计文档确定。

## 13. 待办 Issue

详细的实施拆分、依赖关系和验收标准见 [Technical Issues（TISSUE）](./docs/tissues/README.md)。

### Issue 1：多文件预处理与索引

需要确定：

- 多文件选择与文件夹批量导入方式。
- 首次处理进度和重复打开时的索引复用策略。
- 源文件发生变化后的自动失效与重新处理机制。
- 索引存储位置、生命周期及是否仅存内存。
- 大型文件的分批读取、取消和错误恢复。

### Issue 2：格式保真原型验证

使用真实 Excel 样本验证：

- 跨行或跨列合并单元格。
- 条件格式、数据验证和批注。
- 图片、图表、形状和数据透视表。
- 公式、外部链接和宏。
- 隐藏行列、冻结窗格、筛选和打印设置。

对比“抽取模式”和“原貌模式”，确定 V1 的默认导出策略及可验收的保真边界。

### Issue 3：无固定表头时的导出上下文

需要确定：

- 仅导出命中物理行是否足够。
- 是否允许用户额外选择命中行的前后若干行。
- 是否需要提供手动指定表头行或上下文区域的能力。
- 同一工作表多个独立表格时如何避免错误复制不相关表头。

### Issue 4：显示值标准化

需要确定并测试：

- 英文大小写规则。
- 日期、时间、百分比、科学计数法和前导零的匹配规则。
- 公式按公式文本还是计算结果搜索。
- 空格、换行和不可见字符的处理规则。
- 错误值及布尔值的搜索表现。

## 14. 当前待确认项

- V1 默认采用“抽取模式”还是“原貌模式”。
- 搜索是否包含隐藏工作表、隐藏行和隐藏列。
- 公式是按可见计算结果搜索，还是同时允许搜索公式文本。
- `.xls`、`.xlsb`、加密文件和受密码保护文件是否需要支持。
- 多关键词的具体输入交互方式。
